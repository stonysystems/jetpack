mode:
  cc: none # concurrency control
  ab: etcd # atomic broadcast
  # read_only: occ
  batch: false
  retry: 20
  ongoing: 1 # per client
  # TEMP 2026-05-27: etcd batch_size=8 + max_undone=300 sweep (revert after)
  etcd_batch_size: 16
  etcd_batch_timeout_ms: 5


#include "commo.h"

namespace janus {

// SwiftPaxosCommo has no protocol-specific implementation; OnPropose drives
// the broadcast directly via the inherited rpc_par_proxies_ map.

} // namespace janus
